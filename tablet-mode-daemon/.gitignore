# Daemon binary (specific to this directory)
/tablet-mode-daemon

# Debug and core files (not in root .gitignore)
*.core
core
vgcore.*

# Patch reject files (not in root .gitignore)
*.rej

# Distribution packages (not in root .gitignore)
*.tar.gz
*.tar.bz2
*.tar.xz
*.zip
